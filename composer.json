{
    "name":        "KazuakiM/bardiche",
    "type":        "library",
    "description": "FTP, FTPS, SFTP and SCP clients",
    "keywords":    [
        "ftp",
        "ftps",
        "sftp",
        "scp",
        "clients"
    ],
    "homepage": "https://github.com/KazuakiM/bardiche",
    "license":  "MIT",
    "authors":  [{
        "name":     "KazuakiM",
        "email":    "kazuaki_mabuchi_to_go@hotmail.co.jp",
        "homepage": "https://github.com/KazuakiM"
    }],
    "require": {
        "php": ">=7.0"
    },
    "require-dev": {
        "phpunit/phpunit": ">=5.7"
    },
    "scripts": {
        "test": [
            "phpunit --colors=always --verbose",
            "php tests/bin/ocular.phar code-coverage:upload --format=php-clover docs/coverage/clover.xml --verbose",
            "php tests/bin/coveralls.phar --config=.coveralls.yml --coverage_clover=docs/coverage/clover.xml --verbose"
        ],
        "docs": [
            "phpunit --colors=always --coverage-clover=docs/coverage/clover.xml --coverage-html=docs/coverage --testdox-html=docs/logging.html --verbose",
            "php tests/bin/apigen.phar generate"
        ]
    },
    "autoload": {
        "psr-4": {
            "KazuakiM\\Bardiche\\": "src/"
        }
    }
}
